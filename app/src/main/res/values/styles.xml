<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomTheme" parent="@android:style/Theme">
        <item name="android:tabWidgetStyle">@style/CustomTabWidget</item>
    </style>
    <style name="CustomTabWidget" parent="@android:style/Widget.TabWidget">
        <item name="android:textAppearance">@style/CustomTabWidgetText</item>
    </style>
    <style name="CustomTabWidgetText"
        parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomSeekBar" parent="@style/Widget.AppCompat.SeekBar.Discrete">
        <item name="android:indeterminateOnly">false</item>
        <!-- <item name="android:progressDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_horizontal</item>-->
        <item name="android:minHeight">4dip</item>
        <item name="android:maxHeight">12dip</item>
        <item name="android:paddingTop">1px</item>
        <item name="android:paddingBottom">1px</item>
        <item name="android:layout_height">5sp</item>
        <item name="android:layout_margin">1sp</item>
    </style>

</resources>
